# 성능·검증 — 알람 (도메인 뷰)

> **출처**: [../성능검증-통합-2026-06-08.md](../성능검증-통합-2026-06-08.md) §2에서 알람 영역만 분할. 교차 caveat·전체 그림은 통합본 참조.
> **근거**: 검증 문서 원문 + 현재 코드 대조. 코드 truth source.

---

## 1. 알람 성능 개선 실측 (PR #114) — ★ 수치

| 지표 | before | after | 근거 |
|---|---|---|---|
| **Celery alarm 태스크 p95** | 460ms | **49ms (↓89%)** | concurrency 2→4 |
| `_send_to_all` broadcast | 순차 | `asyncio.gather` 병렬 → `alarm_stream_lag` 0 유지 | ws_router.py:51 |
| 가스 DRF 저장 | 동기(루프 블로킹) | 비동기 분리 → 이벤트 루프 블로킹 제거 | gas_service.py |

- 코드 확정: `docker-compose.yml:162 --concurrency=4`.
- ⚠ **p95 460→49ms는 커밋 메시지 출처** — Grafana/Celery task duration **1차 evidence 미첨부**. "커밋 기록(추정 아님), evidence 별도 캡처 필요"로 표시.

## 2. 알람 3대 증상 병목 진단 (2026-05-20) — UX/도달성 (수치 아님)

[2026-05-20_[진단]알람-증상-병목.md](2026-05-20_[진단]알람-증상-병목.md). **진단만, 성능 수치 없음** → "검증된 숫자"로 인용 불가, 트러블슈팅 *서사*로만 사용.

| 증상 | 원인(1순위) | 방향 |
|---|---|---|
| A 모달 안 뜸/새로고침해야 옴 | `_AckStore` 24h localStorage 영구 차단 | TTL 24h→60s / RESOLVED 동기삭제 |
| B AI+룰 중복·재연결 누락 | AI mute 60s 만료 후 둘 다 발화 + catch-up이 클라 dedup에 차단 | catch-up cap + bypass 플래그 |
| C 조치완료해야 다음알람 | ack(user-scoped) vs RESOLVED(전역) 멘탈모델 (코드 버그 아님) | UX 통합 (T5 sprint) |

## 3. 알람 큐 계측 (관측 가능성)

- `alarm_stream_lag` 메트릭 — Stream 말단 ID와 커서 시간차(초). 평상시 ≈0, replica 뒤처짐 식별용. (Stream 전환 #107)
- 근거: [2026-06-01_[study]알람Stream전환-3-배포안전장치-메트릭.md](2026-06-01_[study]알람Stream전환-3-배포안전장치-메트릭.md)

## 4. 인용 주의 (알람)

1. #114 알람 p95 460→49ms = 커밋기록(1차 evidence 미첨부).
2. 3대 증상 진단은 **도달성/UX 병목** — 정량 성능치로 쓰지 말 것.
3. 알람 부하 영향(broadcast 병렬·concurrency)은 **부하테스트 after 수치(perf 뷰)에는 미반영** — 06-05 측정이 #114 이전. → [../perf/성능검증-인프라.md](../perf/성능검증-인프라.md) §측정시점 참조.

## 5. 근거 문서 (skill/0608/alarm/, perf/)
- `2026-05-20_[진단]알람-증상-병목.md`, `2026-06-01_[study]알람Stream전환-3-배포안전장치-메트릭.md`
- 부하·throughput 전체는 → [../perf/성능검증-인프라.md](../perf/성능검증-인프라.md)
