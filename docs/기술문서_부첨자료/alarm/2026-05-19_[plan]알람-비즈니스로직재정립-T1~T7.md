# 알람 비즈니스 로직 재정립 — AI 5축 도입 후 (테마 T1~T7)

> **작성일**: 2026-05-19
> **상태**: plan 진입 (분석 완료 → 작업 묶음 7테마 합의). 다음 세션에서 T1 부터 sub-plan 단위로 진행.
> **선행 분석**: [`docs/codereviews/2026_05_19/alarm-business-logic-as-is.md`](../../docs/codereviews/2026_05_19/alarm-business-logic-as-is.md) — 29개 후보 갭 매핑 (§5A 10 / §5B 11 / §5C 8)
> **선행 작업**: 전력 AI 5축 정책 엔진 도입 (브랜치 `feature/power_zscore_cp`, D2/E1/F + 코드리뷰 보강 + 머지 완료)
> **일정 제약**: 시연 리허설 (D-7, 2026-06-07) 전 완료. D-7 이후 touch X (안정성).
> **결정 권한**: 사용자
> **시나리오 선택 (2026-05-19)**: **B — T1 + T6 + T3 만 시연 전, 나머지 (T2/T5/T4/T7) 시연 후 sprint**. 시연 가치 90% 확보 + 안정화 1주 확보.

---

## §0 Context — 왜 본 plan 인가

전력 도메인이 5축 정책 엔진 (Z-score + Change Point 추가) 까지 완성된 시점, **AI 도입 결과가 운영자에게 제대로 전달되는가** 의문 제기됨. As-Is 매핑 결과 29개 후보 갭:

- **§5A 백엔드 동작·정책 갭 (10)** — 발화 조건·우선순위·중복 방지 정책 자체
- **§5B JS-백엔드 인터페이스 미스매치 (11)** — 필드 매핑·인터페이스
- **§5C 알람 팝업 텍스트 구성 (8)** — 운영자가 보는 텍스트 자체

본 plan 은 29개 후보를 **작업 단위 (PR 묶음) 7테마** 로 재구성하고, **시연 가치·비용·일정** 기준 우선순위로 정렬.

### 짝 문서 관계

```
alarm-business-logic-as-is.md (분석)        본 plan (실행)
   §5A/§5B/§5C 분석 분류            ───►    T1~T7 작업 묶음
   (어디서 발견했나)                        (PR 단위로 어떻게 묶을 것인가)
```

---

## §1 작업 묶음 7테마 — 결정 요약 (시나리오 B 적용)

| 순위 | 테마 | 포함 후보 | 시연 가치 | 비용 | 시점 (시나리오 B) |
|---|---|---|---|---|---|
| **1** | **T1. 운영자가 보는 정보 통일** | 5B.2 + 5B.4 + 5C.1 + 5C.2 + 5C.3 + 5C.8 | ⭐⭐⭐ 최대 — algorithm_source 칩이 운영자 핵심 인지 | 중 (~3일, 현실 ~5~7일) | **시연 전** (T6 과 통합 PR) |
| **2** | **T6. 도메인별 메시지 포맷 표준화** | 5C.4 + 5C.5 + 5C.6 | ⭐⭐ — 텍스트 가독성·일관성. T1 과 묶어 진행 | 작음 (~1일) | **시연 전** (T1 과 같은 PR) |
| **3** | **T3. 중복 방지·dedup 의미 통일** | 5A.4 + 5A.5 + 5B.5 + 5B.6 + 5B.10 | ⭐⭐ — 다중 사용자·탭 동작 명확화 | 작음 (~1.5일, 현실 ~2~3일) | **시연 전** (별도 PR) |
| 4 | T2. 위험도 4단계 시각화 | 5A.7 + 5B.7 | ⭐⭐ | 중 (~2~3일) | **시연 후 sprint** |
| 5 | T5. 모달·자동닫힘·이탈 정책 | 5A.6 + 5A.8 + 5A.9 + 5C.7 | ⭐⭐ | 중 (~2~3일) | **시연 후 sprint** |
| 6 | T4. AI vs 룰 알람 분리·통합 결정 | 5A.3 + 5A.10 | ⭐ | 큼 (~4~5일) | **시연 후 sprint** |
| 7 | T7. JS 인프라 정리 | 5B.1 + 5B.8 + 5B.9 + 5B.11 | △ | 중 (~2일) | **시연 후 sprint** |

**시나리오 B 분량**: 시연 전 T1+T6+T3 합 ~8~10일. D-26 ~ D-16 (10일) 안 작업 + D-15 ~ D-1 (14일) 안정화·운영 데이터 누적.

### 묶음 외 항목 (보류 / 시연 후 결정)

| 후보 | 보류 이유 |
|---|---|
| 5A.1 algorithm_source / 5축 라벨이 UI 까지 전달 안 됨 | T1 에 통합 흡수 |
| 5A.2 anomaly_meta 4 필드 UI 활용 0 | T1 에 통합 흡수 |
| 5C.8 algorithm_source 시각 칩 부재 | T1 에 통합 흡수 |
| 5B.1 anomaly_meta JS 미수신 | T7 (시연 후) |
| 5B.8 신규 라벨 JS dict 없음 | T1 일부 적용 + 나머지 T7 |
| 5B.9 gas_anomaly_ai 아이콘 누락 | T7 단순 1줄 추가 |
| 5B.11 CustomEvent 스키마 정리 | T7 (시연 후) |

---

## §2 테마별 상세

### T1. 운영자가 보는 정보 통일 (우선순위 1)

#### 포함 후보
- 5B.2 algorithm_source 토스트 칩 변환 안 됨
- 5B.4 algorithm_source DB 저장 되나 이벤트 패널 미표시
- 5C.1 ML 기술용어 (`score 0.0292, combined=warning`) 노출
- 5C.2 message vs summary 이원화
- 5C.3 토스트/모달/이벤트 패널 fallback 불일치
- 5C.8 algorithm_source 시각 칩 부재 (의도와 같은 항목)

#### Scope

UI 의 알람 표시 단위에서 운영자가 "어떤 축이 발화"·"무엇이 위험" 즉시 인지 가능하도록 통일.

#### 영향 파일

| 파일 | 변경 |
|---|---|
| `fastapi-server/power/services/power_service.py` | summary 의 ML 메타 제거 (score, combined raw) |
| `fastapi-server/gas/services/gas_service.py` | 동일 패턴 (가스 측 summary 정리) |
| `drf-server/apps/alerts/models/alarm_record.py` | `get_short_message()` 가 단일 진실 공급원 — push 측도 이걸 사용 |
| `drf-server/apps/alerts/tasks.py` | push_payload 에 short_message 동봉 (또는 summary 통일) |
| `drf-server/static/js/shared/alarm-mapper.js` | `anomaly_meta.algorithm_source` 매핑 추가 |
| `drf-server/static/js/shared/alarm-popup.js` | 토스트·모달 헤더에 algorithm_source 칩 컴포넌트 추가 |
| `drf-server/static/js/dashboard/panels/event-panel.js` | 이벤트 행에 algorithm_source 칩. fallback (`alarm_type`) 을 사람 친화 라벨로 |
| `drf-server/static/css/components/alarm-chip.css` (신규) | 칩 스타일 (color × icon) |
| `drf-server/static/js/shared/algorithmSourceLabel.js` (신규) | 백엔드 dict 와 동기 (zscore, change_point, IF, ARIMA, combined, night_abnormal) |

#### 결정 필요 의도 (다음 세션 인터뷰)

1. **칩 위치** — 토스트 헤더 vs 본문 prefix vs 양쪽 ?
2. **칩 디자인** — 색상만 / 아이콘+색상 / 색상+한글 라벨 텍스트 (Z-score / 급변 / IF / ARIMA / IF+ARIMA / 야간 가동)
3. **summary 정리 수준** — `(score ..., combined=...)` 만 제거 / 전체 재작성 / 백엔드 0 touch + JS 만 칩 처리
4. **이벤트 패널 fallback** — `alarm_type` → `AlarmType.choices` 의 한국어 label 사용 OK?
5. **gas 측 동시 적용** — 본 T1 에 가스 측도 같이? 또는 가스 담당자 별도 task?

#### 작업 분량 추정
~3일 (백엔드 1일 + JS 1.5일 + CSS·라벨 dict 0.5일)

#### 검증 전략
- 단위: AlarmMapper.fromSensorsAlarm() 의 anomaly_meta 매핑 sanity
- 모듈: AlarmRecord.get_short_message() 출력 검증
- e2e: 라이브 시나리오 (overload / degradation / motor_stuck) 에서 토스트 ·이벤트 패널 ·이력 페이지 텍스트 일관성
- 시각 검증: 운영자 관점 토스트 스크린샷 비교

---

### T6. 도메인별 메시지 포맷 표준화 (T1 과 함께)

#### 포함 후보
- 5C.4 도메인별 prefix 형식 차 (`[IF 이상 감지]` vs `[긴급]` vs `[주의]`)
- 5C.5 정상화 메시지 불통일 ("정상 복귀" vs "위험 해소" 하드코딩)
- 5C.6 숫자 포맷 (`7925.8 W` 천단위 구분 없음)

#### Scope

전력/가스/지오펜스 메시지의 prefix·정상화·숫자 포맷 단일 컨벤션 적용. T1 에서 칩 도입하는 시점에 텍스트 표준화 같이 진행 (PR 통합).

#### 결정 필요 의도

1. **통일 포맷**:
   - 옵션 A: `[위험도·출처] 발생원 현상 (측정값 단위)` — 예: `[위험·IF] 송풍기A 이상 (7,925 W)`
   - 옵션 B: `발생원 현상 (측정값 단위)` + 칩으로 위험도·출처 시각 분리 — 예: `송풍기A 이상 패턴 (7,925 W)` + 칩 `[IF]`
   - 옵션 C: 도메인별 자유 형식 (현 상태) + 컨벤션 가이드만 문서화
2. **정상화 메시지** — 단일 source 결정. 백엔드가 push 또는 JS 하드코딩 통일
3. **숫자 포맷터 위치** — 백엔드 (`format_value(value, unit, decimals=1)`) vs JS

#### 작업 분량 추정
~1일 (T1 과 같은 PR 안에)

---

### T3. 중복 방지·dedup 의미 통일 (우선순위 3)

#### 포함 후보
- 5A.4 3중 중복 방지의 의도 (rate_limit / dedup / cooldown)
- 5A.5 EventAck (user-scoped) vs 클라 dedup 의미 차
- 5B.5 dedup TTL 30s (백) vs 60s (클)
- 5B.6 EventAck (user-scoped) vs dedup (사용자 무관) — 5A.5 와 유사
- 5B.10 event_resolved_at 클라 dedup 미고려

#### Scope

3중·4중 중복 방지 메커니즘의 의미·TTL·범위 일관화. 운영자·다중 사용자·탭 환경에서 동작 명확화.

#### 영향 파일

| 파일 | 변경 |
|---|---|
| `fastapi-server/power/services/power_service.py` | RATE_LIMIT_SEC 조정 (필요 시) |
| `fastapi-server/websocket/services/alarm_queue.py` | PUSH_DEDUP_TTL_SEC 조정 |
| `drf-server/apps/alerts/services/event_service.py` | ALARM_REPOPUP_COOLDOWN_SEC 조정 |
| `drf-server/static/js/shared/alarm-popup.js` | _DEDUP_TTL_MS 통일 + `event_resolved_at` 별도 key |
| 문서 | 4 단계 dedup 의도 명시 (어느 단계가 어떤 의도) |

#### 결정 필요 의도

1. **TTL 통일** — 60s 로 통일? 30s? 도메인별 차등? (운영자 알람 빈도 vs 시연 가시성)
2. **dedup vs ack 의미 분리** — 자동 (dedup) 과 사용자 액션 (ack) 의 운영자 친화 설명
3. **EventAck 사용자별 vs 전체** — 한 사용자 ack 시 dedup 도 강제 설정? 또는 별개?
4. **event_resolved_at** — RESOLVED 토스트 차단 방지 (클라 key 분리)

#### 작업 분량 추정
~1.5일 (코드 변경 작음, 의도 결정·문서 시간 ↑)

---

### T2. 위험도 4단계 시각화 (우선순위 4)

#### 포함 후보
- 5A.7 발화 등급별 표시 차이 (caution/predict_warn/warning/danger UI 압축)
- 5B.7 4단계 combined_risk vs 3단계 risk_level — UI 색상 collapse

#### Scope

STEP 5 권고의 5단계 우선순위 (CRITICAL > ML_ANOMALY > ANOMALY_WARNING > TREND_SHIFT > PREDICTIVE_ALERT) 가 UI 에 가시화. 현재 2 색상 → 4 색상.

#### 영향 파일

| 파일 | 변경 |
|---|---|
| `drf-server/apps/core/constants.py` | RiskLevel 4단계 확장 (CAUTION 추가, TODO 주석 처리됨) — 또는 anomaly_meta.combined_risk 만 활용 |
| `drf-server/static/js/shared/level-mapper.js` | 4 색상 매핑 (`caution: amber`, `predict_warn: yellow`, `warning: orange`, `danger: red`) |
| `drf-server/static/js/shared/alarm-popup.js` | combined_risk 별 색상 적용 |
| `drf-server/static/css/components/alarm-popup.css` | 신규 색상 변수 |

#### 결정 필요 의도

1. **RiskLevel enum 확장 vs anomaly_meta 활용** — 본격 4단계 enum 추가 (DB 마이그레이션) vs UI 만 anomaly_meta.combined_risk 색상 분기 (DB 호환)
2. **caution / predict_warn 의미 명확화** — 운영자에게 차이 어떻게 설명?
3. **색상 팔레트** — 시인성 + 색맹 대응

#### 작업 분량 추정
~2일 (DB enum 확장 시 마이그레이션 + JS 색상 + CSS)

---

### T5. 모달·자동닫힘·이탈 정책 (우선순위 5)

#### 포함 후보
- 5A.6 모달 격상 정책 (60s 무응답 → 차단형)
- 5A.8 Event RESOLVED 자동 close
- 5A.9 페이지 이탈 / 다중 탭 / catch-up
- 5C.7 시간 표시 TimeFormat 의존·KST 명시

#### Scope

운영자가 차단형 모달로 작업 못하거나 catch-up 폭주 같은 정책 측면 정리.

#### 영향 파일

| 파일 | 변경 |
|---|---|
| `drf-server/static/js/shared/alarm-popup.js` | 모달 격상 정책 (60s/disable) / WARNING 자동닫힘 시간 (10s → 60s) |
| `drf-server/apps/alerts/services/event_service.py` 또는 task | RESOLVED 신호 정책 |
| `drf-server/static/js/shared/alarm-ws.js` | catch-up 폭주 방지 (최근 N건만) |
| `drf-server/static/js/shared/TimeFormat.js` (있다면) | KST 명시 |

#### 결정 필요 의도

1. **모달 격상** — 시연 시 disable / 운영 시 enable 모드? 항상 disable?
2. **WARNING 자동닫힘** — 10s 너무 짧음. 30s / 60s / 무한?
3. **catch-up 폭주 cap** — 최근 N건 (10건? 50건?)
4. **KST 표시** — display_time 백엔드 추가 vs JS 측 변환

#### 작업 분량 추정
~2일

---

### T4. AI vs 룰 알람 분리·통합 결정 (우선순위 6 — 의도 결정 필요)

#### 포함 후보
- 5A.3 AI 알람 vs Threshold 룰 알람 중복 가능성
- 5A.10 algorithm_source="" 빈값 케이스

#### Scope

`POWER_ANOMALY_AI` (AI 트랙) vs `POWER_OVERLOAD` (룰 트랙) 분리 의도 재검토. 같은 사건이 두 종류 알람으로 표시되는 게 의도된 것인지.

#### 결정 필요 의도 (가장 큰 의도 결정 — 다음 세션 우선)

1. **분리 유지 vs 통합** 의 정답:
   - 옵션 A: 현 상태 유지 (alarm_type 별도, AI mute 60s 만)
   - 옵션 B: AI 우선 — AI 발화 시 같은 사건의 룰 알람 영구 suppress
   - 옵션 C: 룰 우선 — 룰 발화 시 AI 알람을 격하 또는 묶음
   - 옵션 D: 단일 alarm_type 으로 통합 (algorithm_source 로 출처 분리)
2. **algorithm_source="" 케이스** — POWER_OVERLOAD 로 분류 (룰 측)? 또는 별도 라벨?
3. **변경 시 마이그레이션** — 기존 AlarmRecord 의 분류 처리

#### 작업 분량 추정
~3일 (의도에 따라 변동 큼)

#### 보류 옵션
사용자 의도가 "현 상태 유지 OK" 면 본 테마 시연 후 sprint 로 분리.

---

### T7. JS 인프라 정리 (시연 후 sprint)

#### 포함 후보
- 5B.1 anomaly_meta 4 필드 JS 미수신 (T1 에서 일부 적용, 나머지)
- 5B.8 신규 라벨 JS dict 정의 부재 (T1 에서 일부)
- 5B.9 gas_anomaly_ai 아이콘 누락 (1줄 추가, T1 같이 가능)
- 5B.11 CustomEvent payload 스키마 정리

#### Scope

JS 측 코드 인프라 정리 — fallback chain 줄이기 + 매핑 명시화 + 신규 필드 처리. 시연 영향 작음.

#### 결정 필요 의도

1. **AlarmMapper 스키마 명시화** — JSDoc / TS 도입
2. **listener 가 단일 객체 형식 의존** — fallback chain 제거

#### 작업 분량 추정
~2일 (시연 후 sprint)

---

## §3 일정 매핑 (시나리오 B)

```
2026-05-19 (D-26)
   │ ── 본 plan 머지
   │
2026-05-19 ~ 05-26 (D-26 ~ D-19, 약 7일)
   │ ── T1 + T6 진입 (운영자 정보 통일 + 메시지 포맷 표준화) — 통합 PR
   │ ── PR 머지 → 시연 가시성 확보 (핵심)
   │
2026-05-26 ~ 05-29 (D-19 ~ D-16, 약 3일)
   │ ── T3 진입 (dedup·ack 통일) — 별도 PR
   │
2026-05-29 ~ 06-06 (D-16 ~ D-8, 약 8일)
   │ ── 안정화 + 운영 데이터 누적 + 회귀 검증
   │ ── 시연 시나리오 리허설 (overload / degradation / motor_stuck / night_abnormal)
   │ ── 잔여 버그 / 임계치 튜닝 (Z=3.0 / MEAN_K=3.0 / STD_K=2.0 운영 데이터 기반)
   │
2026-06-06 ~ 06-13 (D-8 ~ D-1, 7일)
   │ ── 시연 리허설 모드 — 본 plan touch X
   │
2026-06-14 (D-day)
   │ ── 시연
   │
2026-06-15 ~ (D+1 ~)
   │ ── 시연 후 sprint 진입:
   │     • T2 (위험도 4단계 색상)
   │     • T5 (모달·자동닫힘·이탈 정책)
   │     • T4 (AI vs 룰 알람 분리·통합 결정) — 운영자 피드백 후
   │     • T7 (JS 인프라 정리)
   │     • IF_ARIMA 적용현황 §3.B (SARIMA / STL / IF feature 확장)
```

각 PR 머지 후 사용자 체크 (매 commit 검토 아님 — PR 단위).

---

## §4 비범위 (본 plan 0 touch)

- AI 모델·5축 엔진 자체 변경 (이미 완성)
- 가스 D1 (Z-score 가스) — 가스 담당자 별도 task
- AlertPolicy 관리 UI
- 작업자 디바이스 (geofence_intrusion worker WS) — 디바이스 결정 후 별도
- 알람 이력 페이지 (`/dashboard/monitoring/events/`) 의 페이지 자체 UX
- 시연 후 sprint 항목 (T7 + IF_ARIMA 적용현황 §3.B/C/D)

---

## §5 다음 세션 진입 가이드

본 plan 으로 다음 세션 진입 시:

### 선행 read 권장
1. 본 plan 의 §1 결정 요약 + §2 T1
2. [`docs/codereviews/2026_05_19/alarm-business-logic-as-is.md`](../../docs/codereviews/2026_05_19/alarm-business-logic-as-is.md) §5A.1·5A.2·5B.2·5B.4·5C.1·5C.2·5C.3·5C.8 (T1 관련)
3. `drf-server/static/js/shared/alarm-popup.js` `_DedupStore`, `AlarmToastStack`, 토스트 헤더·본문 렌더링 함수
4. `drf-server/static/js/shared/alarm-mapper.js` `fromSensorsAlarm` 매핑 함수
5. `fastapi-server/power/services/power_service.py` summary 생성 부분 (L388-396)
6. `drf-server/apps/alerts/models/alarm_record.py` `get_short_message`

### 다음 세션 첫 turn 권장

```
alarm-post-ai-redesign.md 시나리오 B 진입.
T1 의 "결정 필요 의도 5개" + T6 통일 포맷 옵션 답변 후 T1+T6 통합 sub-plan 작성.
```

본 plan 진입 직후 사용자가 답해야 할 핵심 의도 (시나리오 B 기준):
- **T1 결정 5개** — §2 T1 의 "결정 필요 의도"
- **T6 통일 포맷** — A/B/C 옵션
- T2 4단계 enum 확장 — 시연 후 sprint 진입 시 결정 (본 plan 진입 시 X)

### 다음 세션이 작성할 것
- T1 sub-plan (skill/plan/alarm-t1-info-unification.md 같은)
- 각 PR 별 적용 보고 (drf-server/docs/refactoring/)
- 코드 리뷰 흐름 (docs/codereviews/<date>/)

---

## §6 학습 시연 가치 — T1~T6 완료 시점

본 plan 완료 시 운영자가 시연 시 보는 효과:

| 영역 | Before (현재) | After (T1~T6 완료) |
|---|---|---|
| 토스트 라벨 | `[IF 이상 감지] 송풍기A watt=7925.8 (score 0.0292, combined=warning)` | 칩 `[IF]` + `송풍기A 이상 패턴 (7,925 W)` |
| 다른 위치 표시 | 토스트/모달/이력 페이지 텍스트 다름 | 통일된 short_message + 칩 |
| 위험도 색상 | 2 색상 (warning/danger) | 4 색상 (caution/predict_warn/warning/danger) |
| 5축 출처 표시 | summary 안에 묻힘 | 시각 칩 (IF / Z-score / 급변 / ARIMA / 야간 가동) |
| dedup / ack 동작 | 다중 사용자·탭 모호 | TTL 통일 + 의도 명시 |
| 모달 차단 | 60s 무응답 모달 (시연 거추장) | 모드별 정책 (시연 모드 가능) |

→ **STEP 5 권고와 코드의 1:1 일치** 가 운영자 UI 까지 전달됨. 학습 자료 가치 ↑.

---

## §7 후속 — 시연 후 sprint (시나리오 B 기준)

| 항목 | 비고 |
|---|---|
| **T2 (위험도 4단계 색상)** | RiskLevel enum 확장 (마이그레이션 필요) 또는 anomaly_meta.combined_risk 만 활용 결정 |
| **T5 (모달·자동닫힘·이탈 정책)** | 시연 시 차단형 모달 부담 피드백 후 결정 |
| **T4 (AI vs 룰 통합)** | 운영자 피드백 후 분리 유지 / 통합 결정 |
| **T7 (JS 인프라 정리)** | 시연 영향 작음. AlarmMapper 스키마 명시화·라벨 dict·아이콘·CustomEvent |
| IF_ARIMA 적용현황 §3.B (SARIMA / STL / IF feature 확장) | [skill/study/IF_ARIMA_적용현황_2026_05_19.md](../study/IF_ARIMA_적용현황_2026_05_19.md) |
| 가스 D1 (Z-score 가스) | 가스 담당자 별도 task |
| 임계치 튜닝 (Z=3.0, MEAN_K=3.0, STD_K=2.0) | 시연 운영 데이터 기반 |

---

## §8 진입 신호

본 plan 은 진입 즉시 가능 — 사용자 결정 2026-05-19 (시나리오 B 채택 — T1+T6 통합 PR + T3 별도 PR 시연 전, 나머지 시연 후). 다음 세션이 본 plan §5 가이드 따라 T1 부터 진행.

**진입 우선 액션**: T1 의 "결정 필요 의도 5개" + T6 통일 포맷 옵션 인터뷰 → T1+T6 통합 sub-plan 작성 → PR 진행.
