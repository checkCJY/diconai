# 알람 운영자 메시지 텍스트 정리 + 인프라 일관성 (T1+T6)

작성일: 2026-05-19
브랜치: `feature/0519_alarm_business_logic`
부모 plan: [skill/plan/alarm-t1-t6-info-unification.md](../../../skill/plan/alarm-t1-t6-info-unification.md) (T1+T6 sub-plan, 부모 [alarm-post-ai-redesign.md](../../../skill/plan/alarm-post-ai-redesign.md))
선행 분석: [docs/codereviews/2026_05_19/alarm-business-logic-as-is.md](../../../docs/codereviews/2026_05_19/alarm-business-logic-as-is.md) §5B.2·5B.4·5C.1·5C.2·5C.3·5C.4·5C.5·5C.6

---

## 1. 왜 본 작업인가

전력 AI 5축 정책 엔진 (Z-score / Change Point / Isolation Forest / ARIMA / 야간 가동) 까지 도입 완료된 시점, 운영자 UI 에는 여전히 ML 기술용어가 노출되고 있었음.

### Before — 운영자가 보던 토스트
```
┌────────────────────────────────────────────────────┐
│ ⚠ 위험                                              │
│ 압연기                                              │
│ [Z-score 이상 감지] 압연기 watt=3611.3             │
│   (score 0.0195, combined=predict_warn)            │
│ 19:07:02                                           │
└────────────────────────────────────────────────────┘
```

문제점:
- `score 0.0195, combined=predict_warn` — ML 내부 용어 노출. 운영자는 의미 모름
- `watt=3611.3` — 천단위 구분 없음
- 토스트 / 모달 / 이벤트 패널 / 이력 페이지 텍스트 fallback chain 으로 불일치

### After
```
┌────────────────────────────────────────────────────┐
│ ⚠ 위험                                              │
│ 압연기                                              │
│ 압연기 이상 패턴 (3,611.3 W)                       │
│ 19:07:02                                           │
└────────────────────────────────────────────────────┘
```

해결:
- ML 용어 (`score`, `combined=`) 완전 제거
- 천단위 구분 (`3,611.3`)
- 4 위치 (토스트 / 모달 / 이벤트 패널 / 이력) `AlarmRecord.get_short_message()` 단일 진실 공급원
- 이벤트 패널 fallback: 영문 alarm_type 코드 노출 → 한국어 라벨

### 칩 도입 검토 후 제외 (2026-05-19 운영자 검토 결과)

초기 plan 은 algorithm_source (Z-score / 급변 / IF / ARIMA / IF+ARIMA / 야간 가동) 를 시각 칩으로 표시할 예정이었으나, 운영자 검토 시:

- "위험한 알람은 동일하게 대응" 이라는 운영 멘탈 모델에 칩이 추가 학습 부담 발생
- 화면 설계서에 칩이 정의되어 있지 않음
- 알고리즘 출처는 엔지니어 진단 영역이지 운영자 의사결정 영역 아님

→ **칩 도입 제외 결정.** 본문 텍스트 정리·인프라 일관성·fallback 라벨만 시연 전 적용. 5축 정책 엔진의 가치는 별도 시연 채널 (운영 데이터 누적 + Grafana 대시보드) 로 입증.

---

## 2. 결정 사항 (인터뷰 2026-05-19)

| # | 항목 | 결정 |
|---|---|---|
| 1 | T6 통일 포맷 | 본문 깔끔 텍스트 — `{label} 이상 패턴 ({value:,.1f} W)` |
| 2 | T1 칩 위치 | 칩 제외 (운영자 검토) |
| 3 | T1 칩 디자인 | 칩 제외 |
| 4 | T1 summary 정리 | 옵션 2 — 전체 재작성 + T6 포맷 |
| 5 | T1 이벤트 패널 fallback | AlarmType 한국어 라벨 (constants.py L33-67 동기) |
| 6 | T1 가스 측 범위 | 전력만 텍스트 변경. 가스는 push 인프라만 표준화 (가스 담당자 PR 영역 보호) |
| 7 | T6 정상화 메시지 source | 백엔드 push 통일 (`get_short_message()`) |
| 8 | T6 숫자 포맷 | Python f-string `{:,.1f}` + JS `toLocaleString()` (기존 공용 코드 활용) |

---

## 3. 변경 영향 요약

### 데이터 (DB 스키마)
**마이그레이션 0건.** `algorithm_source` 필드는 0018 마이그레이션에서 이미 추가됨 (DB 저장 자체는 유지, UI 노출만 제외). push_payload JSON 에 `message` 필드 추가 (스키마 X, payload 키 확장).

### 백엔드

| 파일 | 변경 |
|---|---|
| `fastapi-server/power/services/power_service.py` | summary 재작성 (`[label] 이상 패턴 ({value:,.1f} W)`). ML 용어 제거. `_ALGORITHM_SOURCE_LABEL` dict orphan 제거. push_payload 에 `message` 필드 동봉 |
| `fastapi-server/gas/services/gas_service.py` | push_payload 에 `message` 필드 동봉 (가스 텍스트 내용 0 변경, 인프라 일관성만) |
| `drf-server/apps/alerts/models/alarm_record.py` | `get_short_message()` 전력 AI 분기 알고리즘 라벨 제거 + 천단위 포맷. 정상화 분기 (`power_clear` / `gas_clear`) 에 device/gas prefix 추가 |

### 프론트엔드

| 파일 | 변경 |
|---|---|
| `drf-server/static/js/dashboard/panels/event-panel.js` | `LABEL_BY_TYPE` dict 추가 + main fallback + clear group fallback 에 alarm_type 한글 라벨 |

(칩 관련 신규 파일·alarm-mapper·alarm-popup 변경은 운영자 검토 결과 제외)

---

## 4. 시그니처 변경 (호출자 영향)

### `AlarmRecord.get_short_message()` — 반환값 변경

| alarm_type | Before | After |
|---|---|---|
| `power_anomaly_ai` (with prefix) | `"송풍기A IF 이상 감지 (7925.8 W)"` | `"송풍기A 이상 패턴 (7,925.8 W)"` |
| `power_anomaly_ai` (no prefix) | `"AI 이상 감지 (7925.8 W)"` | `"이상 패턴 (7,925.8 W)"` |
| `power_overload` | `"전력 임계치 초과 (15.8 W)"` | `"전력 임계치 초과 (15.8 W)"` (천단위 적용, 같은 값) |
| `power_clear` (with channel) | `"정상 복귀"` | `"송풍기A 정상 복귀"` |
| `power_clear` (no channel) | `"정상 복귀"` | `"정상 복귀"` |
| `gas_clear` (with gas_type) | `"정상 복귀"` | `"CO 정상 복귀"` |
| `gas_clear` (no gas_type) | `"정상 복귀"` | `"정상 복귀"` |
| `gas_threshold` / `gas_anomaly_ai` | 변경 X | 변경 X (가스 담당자 PR 영역 보호) |

호출처:
- `AlarmRecordSerializer.get_message` — API 응답
- `_push_to_ws` in `tasks.py` — WS push payload

테스트 영향: `test_alarm_message_parity.py` 1개 케이스 expected 값 업데이트 + 신규 케이스 1개 (천단위 포맷 검증).

### `fastapi push_payload` — 키 추가

| 필드 | Before | After |
|---|---|---|
| `message` | 없음 | `summary` 와 동일 텍스트 (가스/전력 공통) |

JS `AlarmMapper.fromSensorsAlarm()` 의 fallback chain (`src.message || src.summary`) 가 이미 양쪽 지원 — 기존 호출자 무영향.

### `EventPanel.LABEL_BY_TYPE` — 신규 dict

constants.AlarmType 12종 한국어 label 과 동기. main label fallback / clear group source fallback 양쪽에서 사용. 영문 코드 노출 회피.

---

## 5. 라이브 시나리오별 동작

| # | 시나리오 | 토스트 본문 |
|---|---|---|
| 5.1 | 전력 IF 단독 (overload) | `송풍기A 이상 패턴 (7,925.8 W)` |
| 5.2 | 전력 ARIMA 단독 (degradation) | `송풍기A 이상 패턴 (820.5 W)` (예측 트랙도 동일 포맷) |
| 5.3 | 전력 IF+ARIMA 동시 (motor_stuck) | `송풍기A 이상 패턴 (12,500.0 W)` |
| 5.4 | 야간 가동 (KST 22~05 baseline 초과) | `송풍기A 이상 패턴 (350.2 W)` |
| 5.5 | 전력 정상 복귀 | `송풍기A 정상 복귀` (이전: `정상 복귀`) |

5.1~5.4 모두 동일 텍스트 포맷 — algorithm 출처는 본문에 노출 안 됨 (DB `algorithm_source` 컬럼에는 저장됨, 엔지니어가 SQL/Grafana 로 추적 가능).

---

## 6. 검증

### 회귀
| 영역 | 테스트 수 | 결과 |
|---|---|---|
| `drf-server apps/alerts/tests/` | 54 | ✅ |
| `fastapi-server tests/` | 173 | ✅ |
| `test_alarm_message_parity.py` | 14 (신규 1 추가) | ✅ |

### E2E (수동)
- Docker compose 실행 (drf+fastapi+celery+redis+prom+grafana healthy)
- 5 시나리오 토스트/모달/이벤트 패널 텍스트 시각 비교 — 시연 리허설 (D-7) 까지 운영 데이터 누적 후 재검증

---

## 7. 잔여 작업 (시연 후 sprint)

| 항목 | 비고 |
|---|---|
| T2 — 위험도 4단계 색상 | RiskLevel enum 확장 (마이그레이션) vs anomaly_meta.combined_risk 분기 결정 |
| T3 — dedup·ack 통일 | TTL 통일 (60s/30s) + EventAck 의미 명확화. plan §1 #3 |
| T4 — AI vs 룰 분리·통합 결정 | 운영자 시연 피드백 후 |
| T5 — 모달·자동닫힘·이탈 정책 | 60s 무응답 격상 / WARNING 자동닫힘 / catch-up 폭주 cap |
| T7 — JS 인프라 정리 | AlarmMapper 스키마 명시화 + gas_anomaly_ai 아이콘 1줄 + CustomEvent 스키마 |
| 가스 측 summary 정리 | 가스 담당자 별도 PR (gas_service.py L153, L161) |
| algorithm_source 시각화 (재검토) | 운영자 피드백 후. 칩이 아닌 다른 방식 (예: 진단용 별도 페이지) 가능 |

---

## 8. Commit 분할

| # | Commit | 핵심 |
|---|---|---|
| C1 | `feat(alarm): T1 summary·정상화 메시지 정리` | power_service.py summary 재작성. alarm_record.py 정상화 prefix |
| C2 | `feat(alarm): T1 fastapi push_payload 에 message 필드 동봉` | 전력 + 가스 push 인프라 일관성 |
| C5 | `feat(alarm): T1 이벤트 패널 fallback — alarm_type 한글 라벨` | LABEL_BY_TYPE dict (constants.AlarmType 동기) |
| revert | `revert(alarm): T1 칩 컴포넌트 제거 (운영자 검토 결과)` | C3·C4 변경 surgical 롤백. 운영자 멘탈 모델·화면 설계서 정렬 |

C3·C4 는 commit 이력에는 남으나 코드는 revert commit 에서 제거. 본 PR 의 net diff 는 C1+C2+C5 + revert 적용 결과.
